<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no">
	<style>
		.qrcode-img{
			width: 300px;
			height: 600px;
		}
		.no-img{
			color: red
		}
	</style>
</head>
<body>
	<%if @img.present?%>
		<%@img.each do |img|%>
			<div><img class="qrcode-img" src="<%=img.path.url%>" /></div>
		<%end%>
	<%else%>
		<h2 class="no-img">暂无图片</h2>
	<%end%>
	<%=javascript_include_tag 'jquery'%>
</body>
</html>
